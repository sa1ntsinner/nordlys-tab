# Publishes site/ to GitHub Pages, with the extension's own sky scripts and
# fonts copied in beside it by tools/site-build.cjs.
name: Site

on:
  push:
    branches: [main]
    paths: ["site/**", "src/js/background.js", "src/js/sky-*.js", "src/js/colour-tools.js", "src/fonts/**", "icons/**", "tools/site-build.cjs", ".github/workflows/pages.yml"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node tools/site-build.cjs
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - id: deployment
        uses: actions/deploy-pages@v4
